# 小店预约排班系统实施文档（给 GitHub Copilot 的执行说明）

## 0. 核心执行约束（最高优先级）

1. 只要有任何需求不明确、字段不明确、权限不明确、流程不明确，必须马上提问用户澄清。
2. 在没有得到用户明确肯定回答之前，禁止进行任何代码修改。
3. 禁止猜测需求，禁止自行补全业务规则。
4. 每次准备改代码前，先输出“本次改动范围、影响模块、验证方式”，等待用户确认后再执行。
5. 如果发现需求互相冲突，立即停止实现并提问，不得继续推进。
6. 每完成一个阶段后，先汇报结果和阻塞点，再请求下一步确认。
7. 全程使用 Shop Calendar Specialist 这个 agent。
8. GitHub Copilot 在本项目中只充当“调度器”，只负责调用 agent 和回传结果，不直接执行实现工作。
9. 任何后续对这个项目的代码修改、调试、验证、文档更新、需求补充，都必须由 Shop Calendar Specialist agent 执行，禁止绕过 agent。
10. 每次开始新任务时，必须先声明“当前调用 Shop Calendar Specialist agent + 本轮目标”。
11. 在用户未明确授权前，agent 只能做澄清和计划，不得进入实现。
12. 一旦发生“未通过 agent 的直接操作”，必须立即停止并回到 agent 澄清流程。
---

## 1. 项目目标

构建一个用于普通小店的预约排班系统，使用类似 Teams Calendar 的交互体验，满足以下业务目标：

1. 员工可以创建和移动预约。
2. 管理员可以创建员工账号、停用员工账号。
3. 所有人看到相同的日历内容（当前权限规则）。
4. 系统简洁清晰，优先低成本上线，可部署到低配 VPS。
5. 先使用免费技术方案，后续按真实需求扩展。

---

## 2. 已确认需求（冻结）

1. 团队规模：3-5 名员工，同时操作一般不超过 3 人。
2. 部署环境：已有 5 美元/月 VPS，先做可行性验证。
3. 时间粒度：30 分钟。
4. 预约必填字段：电话、时间、价格。
5. 预约选填字段：备注、客户名、服务项目（允许先合并到备注）。
6. 冲突规则：同一员工同一时间段不可重复预约，必须拦截。
7. 角色：管理员、员工两类，结构要可扩展。
8. 账号管理：管理员手动创建并分配账号，员工自行登录。
9. 可见性：员工可看到其他员工内容（统一视图）。
10. 时区：以美国中部时区为主。
11. 商业策略：不为单一高级日历视图支付 Premium 年费。
12. 电话格式：仅数字，长度 10-15 位。
13. 价格单位与精度：USD，保留两位小数。
14. 默认预约时长：60 分钟。
15. 删除策略：员工只能取消状态；管理员可删除，且保留追溯。
16. 员工编辑权限：员工可编辑全部预约，且必须记录审计日志。
17. 历史数据保留期限：永久保留。

---

## 3. 选型结论（免费优先）

### 3.1 前端

1. React + TypeScript
2. FullCalendar 免费版（Standard）
3. UI 组件库：Ant Design（或 Mantine，二选一）
4. 数据请求与缓存：TanStack Query

### 3.2 后端

1. NestJS + TypeScript
2. Prisma ORM
3. PostgreSQL
4. 鉴权：JWT
5. 权限：RBAC（当前两角色，数据库结构预留扩展）

### 3.3 部署

1. Docker Compose
2. Nginx 反向代理
3. HTTPS 证书
4. PostgreSQL 定时备份

---

## 4. 为什么不用 FullCalendar Premium（当前阶段）

1. Premium 的资源时间轴（按员工横向排班）确实更直观，但这是付费能力。
2. 当前团队规模小，免费版通过“周/日视图 + 颜色区分 + 员工筛选”可满足核心业务。
3. 当前最应该投入的是业务正确性（冲突、权限、时区、审计），不是高级视图。
4. 后续如果员工规模或排班复杂度提升，再评估升级 Premium，避免前期过度投入。

---

## 5. MVP 功能边界

### 5.1 必做

1. 登录/登出
2. 管理员创建员工账号
3. 管理员停用员工账号
4. 预约新增/编辑/删除
5. 预约拖拽移动时间
6. 冲突检测并拦截
7. 30 分钟粒度选择
8. 必填校验（电话、时间、价格）
9. 全员统一日历可见
10. 审计日志（记录关键变更）

### 5.2 暂不做

1. 短信/微信通知
2. 客户自助预约端
3. 多门店统一后台
4. 高级资源时间轴
5. 薪资考勤联动

---

## 6. 数据模型（第一版）

### 6.1 users

- id
- username
- password_hash
- display_name
- role (admin/employee)
- status (active/inactive)
- created_at
- updated_at

### 6.2 appointments

- id
- employee_id
- start_at
- end_at
- phone (required)
- price (required)
- customer_name (optional)
- service_name (optional)
- note (optional)
- created_by
- updated_by
- created_at
- updated_at

### 6.3 audit_logs

- id
- actor_user_id
- action
- entity_type
- entity_id
- before_payload
- after_payload
- created_at

说明：RBAC 可扩展表（roles, permissions, role_permissions）建议在第一版就建好，即使先只用两个角色。

---

## 7. 核心业务规则

1. 冲突检测规则：同一员工预约时间段重叠时，拒绝创建或修改。
2. 时间粒度规则：开始和结束时间必须是 30 分钟对齐。
3. 必填规则：电话、时间、价格缺失则拒绝保存。
4. 账号状态规则：inactive 员工禁止登录和操作。
5. 可见性规则：管理员和员工均可查看全量预约。
6. 时区规则：前后端统一使用美国中部时区显示，数据库采用规范化时间存储。
7. 编辑与审计规则：员工可编辑其他员工预约，但必须记录审计日志，日志至少包含修改人、修改时间、修改前后关键字段。

---

## 8. API 设计建议（MVP）

### 8.1 auth

1. POST /auth/login
2. POST /auth/refresh
3. POST /auth/logout

### 8.2 users

1. GET /users
2. POST /users
3. PATCH /users/:id/status
4. PATCH /users/:id/password

### 8.3 appointments

1. GET /appointments
2. POST /appointments
3. PATCH /appointments/:id
4. DELETE /appointments/:id
5. POST /appointments/:id/move（可选，也可合并到 PATCH）

---

## 9. 前端页面结构（免费版可落地）

1. 登录页
2. 日历主页
   - 顶部工具栏：日期跳转、周/日切换、员工筛选
   - 日历区域：FullCalendar（timeGridWeek/timeGridDay）
   - 预约弹窗：新增/编辑
3. 管理员页面
   - 员工列表
   - 新建员工
   - 停用/启用账号

视觉替代策略（替代 Premium 资源时间轴）：

1. 每个员工固定颜色
2. 预约卡片显示员工名
3. 提供员工筛选器
4. 提供今日预约列表辅助查看

---

## 10. 详细执行步骤（Step by Step）

## 阶段 A：需求确认与冻结

目标：确保没有任何猜测项。

执行：

1. 输出需求清单与未决问题。
2. 所有未决问题逐条提问用户。
3. 用户确认后，更新需求冻结文档。

通过标准：

1. 未决问题数量为 0。
2. 用户明确同意进入开发。

## 阶段 B：后端基础搭建

目标：建立可扩展、可部署的后端骨架。

执行：

1. 创建 NestJS 项目结构。
2. 配置 Prisma + PostgreSQL。
3. 建立 auth/users/appointments 模块。
4. 增加统一错误处理和日志。

通过标准：

1. 健康检查接口可用。
2. 数据库连接稳定。

## 阶段 C：认证与权限

目标：先把账户体系跑通。

执行：

1. 实现管理员和员工登录。
2. 实现 JWT 鉴权。
3. 实现基础 RBAC 守卫。

通过标准：

1. 员工无法访问管理员接口。
2. 停用账号无法登录。

## 阶段 D：预约核心能力

目标：保证预约正确性。

执行：

1. 实现预约 CRUD。
2. 实现 30 分钟粒度校验。
3. 实现必填字段校验。
4. 实现同员工冲突检测。

通过标准：

1. 重叠预约 100% 被拦截。
2. 错误提示清晰可读。

## 阶段 E：前端日历集成

目标：完成可用日历工作流。

执行：

1. 集成 FullCalendar 免费版。
2. 实现新增/编辑/删除预约弹窗。
3. 实现拖拽移动预约并联调后端。
4. 冲突时前端提示并回滚 UI。

通过标准：

1. 日历端到端功能完整可用。
2. 冲突和校验反馈准确。

## 阶段 F：部署与运维

目标：在 VPS 稳定运行。

执行：

1. Docker Compose 编排服务。
2. Nginx + HTTPS。
3. 环境变量与密钥管理。
4. 数据库备份任务。

通过标准：

1. 线上可访问。
2. 可恢复备份。

---

## 11. 测试清单（必须覆盖）

1. 登录成功/失败/停用账号
2. 管理员创建员工、停用员工
3. 员工权限越权访问拦截
4. 预约新增/编辑/删除
5. 拖拽移动预约
6. 冲突检测（重叠与边界时段）
7. 30 分钟粒度校验
8. 必填字段校验
9. 时区边界测试（跨日、夏令时）
10. 2-3 人并发编辑冲突验证
11. 员工编辑他人预约成功且日志完整
12. 管理员删除/员工取消 的权限与日志验证
13. 默认时长 60 分钟验证
14. 电话格式（10-15 位数字）与价格精度（USD，两位小数）验证

---

## 12. 风险与应对

1. 风险：需求变化导致返工
   - 应对：每阶段前强制确认，不确认不编码。
2. 风险：低配 VPS 资源不足
   - 应对：先控制范围，开启监控，必要时升级规格。
3. 风险：时区和夏令时错误
   - 应对：统一时区策略 + 边界测试用例。
4. 风险：未来改资源时间轴成本不可控
   - 应对：前端做“日历组件适配层”，避免业务逻辑耦合到具体视图。

---

## 13. 面向 Copilot 的执行提示词模板

以下模板用于每次进入新任务时，先约束 Copilot 的行为：

"""
你现在要执行本项目任务。请严格遵守：
1. 如果有任何不明确地方，马上提问我澄清。
2. 在我明确确认之前，不要修改任何代码。
3. 不要做任何猜测。
4. 你必须通过 Shop Calendar Specialist agent 执行所有项目动作，Copilot 只负责调度与转述。
5. 先列出你理解的需求和本次改动范围，等我确认。
6. 修改后给出变更摘要、验证步骤、潜在风险。
"""

---

## 14. 已收敛决策（已确认）

1. 电话格式：仅数字，长度 10-15 位。
2. 价格单位与精度：USD，保留两位小数。
3. 默认预约时长：60 分钟。
4. 删除策略：员工只能取消状态；管理员可删除，且保留追溯。
5. 员工编辑权限：员工可编辑全部预约，且必须有审计日志。
6. 历史数据保留期限：永久保留。

---

## 15. 里程碑建议（3 周）

1. 第 1 周：后端骨架 + 登录权限 + 员工管理
2. 第 2 周：预约核心 + 冲突检测 + 日历联调
3. 第 3 周：部署上线 + 审计日志 + 回归测试

---

## 16. 当前决策总结

1. 先用免费版 FullCalendar 上线，不购买 Premium。
2. 以业务正确性优先，不为单一高级视图增加年费成本。
3. 保留后续升级路径，但不提前付费。
4. 严格执行“先澄清后编码”。

---

## 17. 固定回复语句模板（用户直接复制给 Agent）

使用方法：

1. 选中对应场景。
2. 直接复制整段发给 Agent。
3. 除“授权修改”模板外，默认都不允许改代码。

### 17.1 会话开场（强约束）

"""
请严格按 COPILOT_IMPLEMENTATION_PLAN.md 执行本项目。
你只能通过 Shop Calendar Specialist agent 执行本项目动作，Copilot 不得直接改代码。
先完成三件事：
1. 复述已确认需求。
2. 列出所有未确认项（必须提问，不可猜测）。
3. 给出本轮最小工作范围。
在我明确批准之前，禁止任何代码修改。
"""

### 17.2 进入需求澄清阶段（只问不做）

"""
现在只进入“阶段 A：需求确认与冻结”。
要求：只提问和整理，不写代码、不改文件。
输出格式：
1. 已确认
2. 未确认问题清单
3. 建议默认值（仅建议，不落地）
"""

### 17.3 你回答完澄清问题后（让 Agent 收敛结论）

"""
我已回答你的澄清问题。
请输出：
1. 更新后的需求冻结清单
2. 本轮可执行范围
3. 仍存在的风险和待确认点
注意：此步仍不允许改代码。
"""

### 17.4 让 Agent 先出改动计划（仍不授权）

"""
请先给“本次改动计划”，暂不执行代码修改。
计划必须包含：
1. 将修改的文件
2. 每个文件改动目的
3. 影响模块
4. 验证步骤
5. 回滚方案
"""

### 17.5 正式授权修改代码（唯一可改代码模板）

"""
我批准你按刚才的计划执行。
你现在可以调用 Shop Calendar Specialist agent 开始执行修改。
要求：
1. 只改计划内范围
2. 若出现新不确定项，立即暂停并提问
3. 完成后给出变更摘要和验证结果
"""

### 17.6 修改后收尾汇报模板

"""
请按以下结构汇报本轮结果：
1. 实际修改内容
2. 已满足的需求点
3. 测试步骤与结果
4. 已知风险/技术债
5. 下一步建议（最多 3 条）
"""

### 17.7 发现跑偏时的纠偏模板

"""
停止当前执行。
你出现了“未确认先实现”或“需求猜测”问题。
请回到澄清模式：
1. 列出不明确点
2. 逐条提问
3. 等我确认后再继续
当前不允许代码修改。
"""

### 17.8 每个阶段结束后的通用回复模板

"""
本阶段我已确认。
请给出下个阶段的：
1. 目标
2. 最小执行范围
3. 需要我先确认的问题
在我确认前，不要改代码。
"""

### 17.9 极简通用模板（一句话版本）

"""
按 COPILOT_IMPLEMENTATION_PLAN.md 执行：Copilot 只调度 Shop Calendar Specialist agent；先澄清、后计划、等我批准、再实施；任何不明确先问，不得猜测，不得绕过 agent 直接改代码。
"""

## 18. 违规检查清单（每轮执行前必检）

1. [ ] 已确认 Copilot 仅做调度与回传，不直接实现。
2. [ ] 已确认本轮必须调用 Shop Calendar Specialist agent。
3. [ ] 已确认未获授权前不进入实现与代码修改。
4. [ ] 已确认流程为先澄清、后计划，再等待批准执行。
5. [ ] 已确认凡是不明确项，先提问，不猜测、不补全。
6. [ ] 已确认不绕过 agent，不进行任何直接操作。
7. [ ] 已确认阶段结束先汇报结果与阻塞，再请求下一步确认。
8. [ ] 已确认一旦发现需求偏差，立即停止并回到澄清。
9. [ ] 已声明本轮目标与范围，仅在已确认边界内推进。

### 18.1 每轮开始时的固定声明模板

1. 正在调用 Shop Calendar Specialist agent 执行本项目任务。
2. 本轮目标：____________。
3. 当前阶段：____________。
4. 是否进入实现：否（默认不进入实现，待你明确授权）。
5. 请确认以上目标与范围，确认后我再继续下一步。

## 19. 阶段 A 完成判定与下一步执行包

### 19.1 阶段 A 完成判定

1. 阶段 A 未决问题数量已收敛为 0，不存在待澄清阻塞项。
2. 已确认并冻结的 6 项决策全部一致，无内部冲突。
3. 需求边界、权限边界、字段边界已形成可执行输入。
4. 执行约束已明确：先对齐实现、后推进编码，不新增业务规则。
5. 当前状态满足“进入实现准备”的门槛，可切换到最小实现对齐轮次。

### 19.2 下一轮最小执行范围（进入实现前）

1. 本轮原则：不改业务规则，只做实现对齐。
2. DTO 校验对齐：对齐电话（10-15 位数字）、价格（USD 两位小数）、时间与必填字段校验口径。
3. 删除/取消权限分流：落实“员工仅取消状态、管理员可删除并保留追溯”的接口与约束对齐。
4. 员工编辑他人预约的审计日志：补齐并核对日志字段完整性与触发时机。
5. 测试补充与文档对齐：补齐第 11 节新增 4 项测试，并同步 README/API 文档到已冻结规则。

### 19.3 本轮改动范围、影响模块、验证方式（待批准）

1. 改动范围（仅文件类别，不写代码）：DTO 定义文件、控制器/服务实现文件、审计记录相关文件、测试文件、README 与 API 说明文档。
2. 影响模块：auth、users、appointments、audit、frontend（仅联调与文档一致性，不扩展新功能）。
3. 验证方式：build 校验、单元测试、关键 e2e 用例、手工冒烟验证（登录、预约编辑、删除/取消、审计留痕）。

### 19.4 用户批准模板（复制即用）

1. 我已确认进入“实现前最小对齐轮次”。
2. 本轮仅执行第 19.2 条所列范围。
3. 仅按19.2范围执行，超出即暂停提问。
4. 不新增、不修改任何业务规则定义。
5. 请先执行并完成 19.3 所列验证方式。
6. 完成后按“改动摘要 + 验证结果 + 风险项”结构汇报。

## 20. Execution Hook / Startup Checklist

Session start self-check (must run before any implementation action):

1. Read `COPILOT_IMPLEMENTATION_PLAN.md` from top to bottom before the round starts.
2. Declare: "Using Shop Calendar Specialist agent" and state the round goal.
3. Verify unresolved ambiguity (requirements, fields, permissions, flow).
4. If anything is unclear, stop and ask clarification questions first.
5. Do not modify code until explicit user confirmation is received.
6. Ensure all implementation actions are executed via Shop Calendar Specialist agent only; Copilot acts as dispatcher and reporter only.
7. If any bypass happens, stop immediately and return to clarification flow.
8. Confirm scope, impacted modules, and validation method before editing.
9. Execute only confirmed scope; if new uncertainty appears, pause and ask.
